name: fixed_spacer
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/L1malucas/fixed-spacer

environment:
  sdk: '>=3.0.0-290.0.dev <4.0.0'

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
